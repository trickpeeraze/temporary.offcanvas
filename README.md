temporary.offcanvas
===================

A jQuery plugin for magic canvas on your web.

## Development

This project now uses [Vite](https://vitejs.dev/) for building the library.

Install dependencies with `npm install` and run `npm run build` to produce the distribution files in `dist/`.

Go to this [web](http://offcanvas.temporarytrick.com/) for Usage.
